# Changelog

Generated automatically by `.github/workflows/release.yml` on every push to `main`, from `git log` since the
previous release tag. Do not hand-edit above the first `##` entry the workflow adds — it prepends new sections
here, it does not merge with hand-written ones.
